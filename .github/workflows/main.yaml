name: 'My First GitHub Actons'

on: push

env:
  PRESET_VALUE: "This is the value preset"

jobs:
  first_job:
    name: 'First Job'

    strategy:
      matrix:
        os: [ 'widnows-lasest', 'maxos-latest', 'ubuntu-latest']

    runs-on: ${{ matrix.os }}

    steps:
      - name: Say Hello World from ${{ matrix.os }}
        shell: bash
        run: |
          echo "Hello, from ${{ matrix.os }}"